# Ailernova — open bugs and tasks

**4 September 2026** · `main` at `e81013e` · Render deployed `307f2b8` · Play build 6 (`fed845f`, 21 Aug), **75 commits behind**

Every row below was checked against the live database, the deployed server, or the
code — not carried over from the previous list. Items fixed since 31 August have
been dropped, not restated.

---

## P0 — blocking users or approval

| # | Bug / task to be fixed | Priority | Est. hours |
|---|---|---|---|
| 1 | **Upload a new build to Play.** Build 6 is 75 commits old. Everything below the line — practice for 5 classes, any-class browsing, the access-grant fix, password reset, the tab-lag fix — is invisible to real users until an AAB ships. In-app account deletion, which Play requires for approval, is also only in `main` | P0 | 0.5 |
| 2 | **No mail transport configured.** Password reset is built and tested but sends nothing: neither `SMTP_HOST` nor `RESEND_API_KEY` is set on Render. `/forgot-password` still answers "a reset link is on its way", so the failure is invisible to users and to the app | P0 | 0.25 |
| 3 | **`ELEVENLABS_API_KEY` not set.** Every AI-teacher lesson falls back to the device voice. There is no fallback chain by design, so one missing key silently changes the teacher's voice for everyone | P0 | 0.25 |
| 4 | **Grievance Officer is a `[NAME]` placeholder** in the privacy policy. Required under Indian law and the policy is already published | P0 | 0.1 |

---

## P1 — content gaps and real limitations

| # | Bug / task to be fixed | Priority | Est. hours |
|---|---|---|---|
| 5 | **No PYQ for classes 7 and 8** (0 of 327 and 0 of 342 chapters), and class 6 has 11 of 292. Needs an examin8 fetch, which is session-gated — blocked on a login | P1 | 8 |
| 6 | **Class 10 revision notes cover 44 of 318 chapters.** Classes 6–9 are fine via `ncert_solutions` part 4; class 10 uses the `sections` model and is mostly empty | P1 | 6 |
| 7 | **Hostinger account lockout.** `ailernova.com` sits in an account nobody can sign into. It blocks Resend domain verification, and the domain **expires 13 October 2026** — after which the site, the Microsoft mail and the API host references all lapse | P1 | 2 |
| 8 | **Web admin cannot grant content access.** `PATCH /api/admin/users/:id/access` exists but no page calls it, so granting outside a support ticket needs the app or a DB write | P1 | 1 |
| 9 | **No Terms of Use page.** Sign-up no longer claims one, so this is a gap rather than a false claim | P1 | 3 |
| 10 | **`maths_questions` is ~29% present in the DB** (biology ~100%, physics ~95%, chemistry ~80%). The bundled copy is the only copy for the rest — it must not be deleted, and the gap should be closed | P1 | 6 |

---

## P2 — quality, size and hygiene

| # | Bug / task to be fixed | Priority | Est. hours |
|---|---|---|---|
| 11 | **Bundle is 51.8 MB.** The reachable question banks ship inside it. Moving them to the API is the fix; the DB now holds 88,950 practice questions, so most of the destination already exists | P2 | 8 |
| 12 | **~215 MB of `src/data` is unreachable from any screen.** Removable only after a both-sides count against the DB, and never for maths | P2 | 3 |
| 13 | **43 empty `catch {}` blocks** across app and server. Errors vanish silently — this is why the chapter-list bug took so long to find | P2 | 2 |
| 14 | **69 `console.log` calls** left in shipping app code | P2 | 0.5 |
| 15 | **Any-class browsing has not been tested on a device.** Server and app are both live; needs a pass across Practice, Resources, MCQ, online and mock tests for each class, confirming content actually changes and the free/paid lock is untouched | P2 | 4 |
| 16 | **Mr. Iker is an illustration, not the HeyGen avatar.** The male teacher is drawn in SVG. Swapping in avatar `214e2370…` needs a HeyGen API key, or any male portrait via `--from` | P2 | 0.5 |
| 17 | **Test data in production.** `grant-test@ailernova.test` and ticket `AL-2549` were created to prove the grant flow and are still live | P2 | 0.1 |
| 18 | **Releases are uploaded by hand.** `eas submit` with a Play service account would stop the next 75-commit gap forming | P2 | 1 |

---

## P3 — needs a product or legal decision first

| # | Bug / task to be fixed | Priority | Est. hours |
|---|---|---|---|
| 19 | **No age gate or verifiable parental consent.** The DPDP Act requires it for under-18s, and most users are children. Product and legal input needed before this can be scoped properly | P3 | 10 |
| 20 | **Support chat reaches nobody outside working hours.** Task 2 made the copy honest; an agent console, routing and presence is the real feature | P3 | 40 |

---

## Anything over 3 hours

| # | Task | Hours | Why it is not shorter |
|---|---|---|---|
| 5 | PYQ for classes 6–8 | 8 | Not code. Content behind a session-gated API, then a mapping to `sections.type_key` and an import run per class |
| 6 | Class 10 revision notes | 6 | Same shape as task 5, smaller scope — 274 chapters short |
| 10 | Close the maths DB gap | 6 | ~71% of `maths_questions` is not in the DB. The bundled files are the source, so this is an import, but the chapter mapping has to be right or it lands under chapters nothing queries |
| 11 | Banks → API | 8 | Migration plus changes to the screens that read them. The DB side is largely done |
| 15 | Cross-class QA | 4 | Seven classes across five surfaces, on a device, checking the paid lock each time |
| 19 | Parental consent | 10 | A product change: age capture, a parent contact step, verification, and a consent record |
| 20 | Support backend | 40 | Agent console, ticket routing, presence, notifications |

---

## Totals

| | Hours |
|---|---|
| P0 | **1.1** |
| P1 | 26 |
| P2 | 18.6 |
| P3 (excluding task 20) | 10 |
| **Everything except the support backend** | **55.7** |

The first hour is worth more than the rest combined: tasks 1–4 are a build upload
and three configuration values, and they unblock work that is already written,
tested and deployed.

---

## Fixed since the 31 August list

Not open work — recorded so the two documents can be compared.

In-app account deletion · support chat no longer implies a person · any-class
browsing (server and app) · `access_level` dropped at sign-in, which made every
granted account still look locked · unlock requests never reaching the support
queue · the Grant button giving no reason when it hid itself · "Select your
profile" asked twice · the Resources tab freezing on 5 MB of eager imports ·
practice content from 16,251 questions to 88,950 · password reset built end to
end · 118 MB of verified-duplicate data removed.
