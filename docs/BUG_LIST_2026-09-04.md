# Ailernova — open bugs and tasks

**4 September 2026**, revised 5 September · `main` at `fd40629` · OTA `9f0d5539` live on `production` · Play build 6 (`fed845f`, 21 Aug), 88 commits behind

Every row was checked against the live database, the deployed server, or the code
— not carried over from the previous list. Items fixed since 31 August are listed
separately at the end rather than restated as open.

---

| # | Bug / task to be fixed | What it is | Priority | Est. hours | Status |
|---|---|---|---|---|---|
| 1 | **Upload a new build to Play** | Build 6 is 88 commits behind, but every commit since is JavaScript, so the 5 September OTA delivers all of it — in-app account deletion included (JS plus a server change already deployed).<br>**Done:** closed by decision — staying on build 6, with the OTA carrying the functionality. One caveat kept on the record rather than as open work: `fallbackToCacheTimeout` is 0, so a fresh install opened ONCE runs build 6's original bundle before the update applies. A Play reviewer checking for the deletion flow on first launch would not find it. AAB version code 9 is built and waiting if that ever needs closing | P0 | 0.5 | Done |
| 2 | **Production cannot send mail at all** | Render blocks outbound SMTP. Port 465 and port 587 both TIME OUT from Render; both work from a laptop with the same credentials, so it is the network and not the configuration. Three code-level causes were found and fixed on the way to this — a trailing newline in `SMTP_HOST` (`726acc2`), the port (`a32d1f6`), and IPv6 with no route from Render (`4b44a2e`) — and each was hiding the next. `/api/health?check=mail` now reports the real error instead of nothing.<br>**Done (8 Sep, `b49431e`):** no SMTP setting could fix it, and no host change was needed either. Probed from an unblocked network, smtp.hostinger.com answers on 465 and 587 ONLY — so the ports Render permits and the ports Hostinger serves do not intersect, and the far end had to change. Now Brevo on **2525**, the one SMTP port Render lets out. The egress probe settles the mechanism: `smtp-relay.brevo.com:2525` open in 80ms while `smtp-relay.brevo.com:587` times out — same host, so it is a port rule, not a host rule. Production reports `{"transport":"smtp","ok":true}`. Still plain SMTP; nodemailer and mailer.js were not touched | P0 | 1 | Done |
| 3 | **`ELEVENLABS_API_KEY` not set** | Every AI-teacher lesson falls back to the device voice. There is no fallback chain by design, so one missing key silently changes the teacher's voice for everyone | P0 | 0.25 | Open |
| 4 | **Switching tabs stalls** | Opening Practice evaluated ~45 MB of question banks on the JS thread before anything could paint, and Resources 5 MB. Both are fixed in `main` (`8171a62`, and the Practice half today) but neither is shipped, so every user still feels it<br>**Done:** Resources `8171a62`, Practice `a82dfd1`, both shipped in the 5 Sep OTA | P0 | 0.5 | Done |
| 5 | **Forgot password does not work end to end** | Every step works except the last: the app calls production, the account is found, a hashed one-time token is written, the reset page serves and accepts a new password (`bcrypt.compare` confirms the change). The email never leaves Render — see row 2.<br>**Open:** the student sees "Check your email" and no email arrives. Proven by triggering a production reset to a mailbox readable over IMAP and finding only the laptop-sent test message in it. 7 Sep: the flow no longer needs a browser — the mail now carries a 6-digit code as well as a link, and ResetPasswordScreen completes the reset in the app (5 attempts per code, then the row is spent; 20 per IP per 15 min). 22 checks pass against the live database. That removed the browser hop; row 2 removed the blocker.<br>**Done (8 Sep):** verified against production — a reset requested from the deployed API issued a hashed code and the mail was accepted for delivery, with SMTP auth confirmed `ok: true`. The student now completes the reset inside the app: 5 attempts per code, then the row is spent; 20 per IP per 15 minutes | P0 | 0.5 | Done |
| 6 | **Grievance Officer placeholder** | The published privacy policy still reads `[NAME]`. Required under Indian law<br>**Done:** The live page reads "Saurabh, saurabh@ailernova.com". My earlier `[NAME]` reading was a cached copy | P0 | 0.1 | Done |
| 7 | **No PYQ for classes 6–8** | Class 7 has 0 of 327 chapters, class 8 0 of 342, class 6 just 11 of 292. Needs an examin8 fetch, which is session-gated — blocked on a login | P1 | 8 | Open |
| 8 | **Class 10 revision notes cover 44 of 318 chapters** | Classes 6–9 are fine via `ncert_solutions` part 4; class 10 uses the `sections` model and is mostly empty | P1 | 6 | Open |
| 9 | **Hostinger account lockout** | `ailernova.com` and `ailernova.in` both sit in a Hostinger account, and the app depends on the second: the privacy policy WebView, 20 parent-app images, a Sessions video and the pricing links all point at it, and `saurabh@ailernova.com` — the contact inside the policy — is Microsoft mail on the domain.<br>**Done:** auto-renewal is set, which removes the 13 October expiry. If SPF and DKIM are ever needed on the domain — sending production mail as `noreply@ailernova.com` from any provider — that needs DNS access on the same account, so it is worth confirming a record can be added before that day arrives rather than on it. | P1 | 2 | Done |
| 10 | **Web admin cannot grant content access** | `PATCH /api/admin/users/:id/access` exists but no page calls it, so granting outside a support ticket needs the app or a DB write<br>**Done:** `aba168b` — grant/revoke on the user page. Needs the admin portal redeployed, which is separate from the app OTA | P1 | 1 | Done |
| 11 | **No Terms of Use page** | Sign-up no longer claims one, so this is a gap rather than a false claim<br>**Done:** `fd40629` — written and shipped in the app, openable from sign-up and Profile. Drafted, not lawyer-reviewed | P1 | 3 | Done |
| 12 | **Maths content mostly missing from the DB** | `maths_questions` is ~29% present, against biology ~100%, physics ~95%, chemistry ~80%. The bundled copy is the only copy for the rest, so it must not be deleted and the gap should be closed | P1 | 6 | Open |
| 13 | **Bundle is 51.8 MB** | The reachable question banks ship inside it. Moving them to the API is the fix; the DB now holds 88,950 practice questions, so most of the destination already exists | P2 | 8 | Open |
| 14 | **~215 MB of `src/data` is unreachable from any screen** | Removable only after a both-sides count against the DB, and never for maths | P2 | 3 | Open |
| 15 | **Errors swallowed silently** | 43 empty `catch {}` blocks across app and server. This is why the chapter-list bug took so long to find<br>**Done:** The count was wrong in the direction that made the job look smaller — it is **50, and none of them are on the server**: 30 in app code, 20 in `src/data/` (bundled banks, leaving with item 14). Built rather than a `console.warn` sweep: an `error_logs` table (`prisma/sql/error_logs.sql`, applied to Supabase 5 Sep), `services/errorLog.service.js`, automatic capture of every 5xx in `middleware/errorHandler.js` (4xx excluded — a wrong password is the API working), batched app ingest at `POST /api/logs/client` behind student auth and a per-user quota with `source` forced server-side so a device cannot file as the server, and an admin reader at `/api/admin/error-logs` behind new `logs.view`/`logs.manage` — held by super_admin and admin, withheld from support and content_manager because stack traces name internal files. App side: `src/utils/errorLog.js` (never throws, never reports itself, buffered and deduped) with 23 of the 30 catches rewritten, plus Error Logs screens under Admin → Profile. **Capped, not merely retained** — 5,000 rows / 14 days / ~2.5 MB, because the database has 17.6 MB of headroom; raise `MAX_ROWS` to 20000 after the Supabase upgrade. Emails and phone numbers are scrubbed before insert. 27 unit tests; the endpoints, the 5xx capture, PII scrubbing, source-forgery rejection and the audited purge were all verified against the live database. ⚠️ **Not in production yet** — only the table exists on Supabase; the code that writes to it ships with the next backend deploy, and the app half with the next build. 7 empty catches remain, all inside HTML injected into a WebView where `reportError` does not exist — in one the catch wraps the very postMessage bridge a report would travel down; they now carry a comment saying so. The screen has not been opened on a device, and the support-role 403 was confirmed in code but never over HTTP. Deploy steps: `docs/superpowers/2026-09-05-error-logs-deploy.md` | P2 | 2 | Done |
| 16 | **Debug logging in shipping code** | 69 `console.log` calls left in the app<br>**Done:** Misleading like item 15's count. Of the 69, **49 are in `src/data/`** — CLI build scripts no screen imports, so Metro never bundles them; they leave with item 14. Another 3 in `utils/teacherVoice.js` were already inside `if (__DEV__)` and 2 in `utils/unlockTrace.js` sit behind `__DEV__ && DEBUG_UNLOCK` with an early return. **Only 12 actually shipped**, in `api/brainGymApi.js` (8), `api/arenaApi.js` (2) and `api/parentApi.js` (2). Seven of those twelve were not debug noise but **silent failure logs** — "matchmake failed — using local match", "questions fetch failed — using local seed" — item 15's problem in promise form. So the five genuine traces were deleted and the seven failures routed into item 15's pipeline: `reportWarn` where the fallback keeps the student whole, `reportError` where it does not (`submitArenaResult` and `submitBrainGymResult` — a score shown to the student that the server never recorded). One more found alongside and fixed with it: `arenaApi.js:fetchActiveMatch` swallowed its error with a bare `return null`. **8 silent failures now reported, 5 traces deleted; unguarded `console.log` in app code is 0.** Same caveat as item 15 — not in production until the next deploy and build | P2 | 0.5 | Done |
| 17 | **Any-class browsing has not been tested on a device** | Server and app are both live; needs a pass across Practice, Resources, MCQ, online and mock tests for each class, confirming content actually changes and the free/paid lock is untouched | P2 | 4 | Open |
| 18 | **Mr. Iker is an illustration, not the HeyGen avatar** | The male teacher is drawn in SVG. Swapping in avatar `214e2370…` needs a HeyGen API key, or any male portrait via `--from` | P2 | 0.5 | Open |
| 19 | **Test data in production** | `grant-test@ailernova.test` and ticket `AL-2549` were created to prove the grant flow.<br>**Done:** removed — 1 user, 1 ticket, 1 message, 5 reset tokens, 0 orphans left behind. It was also sending bounces to support@ailernova.in: `.test` is a reserved TLD that cannot resolve, so every reset touching that account returned "Host or domain name not found" | P2 | 0.1 | Done |
| 20 | **Releases are uploaded by hand** | `eas submit` with a Play service account would stop the next 75-commit gap forming | P2 | 1 | Open |
| 21 | **No age gate or verifiable parental consent** | The DPDP Act requires it for under-18s, and most users are children. Product and legal input needed before this can be scoped properly | P3 | 10 | Open |
| 22 | **Support chat reaches nobody outside working hours** | Task 2 made the copy honest; an agent console, routing and presence is the real feature | P3 | 40 | Open |

**Totals (open only)** — P0 0.25 · P1 20 · P2 16.5 · P3 50 · **86.75 hours**, or **46.75** excluding the 40-hour support backend.

Row numbers never change. A finished item keeps its number and its row, and is marked Done — so a number you quoted last week still points at the same thing.

---

## Where the leverage is

P0 is **0.25 hours** — one unset environment variable. Password reset now works
end to end in production: Render reaches Brevo on 2525, authenticates, and the
student types a 6-digit code into the app without a browser opening.

The lesson worth keeping: three code-level causes were fixed on the way to a
fourth that was not a code problem at all, and each hid the next. The 465 → 587
change was made on a partial measurement and moved between two closed doors.
What settled it was measuring the whole grid — every port against several hosts,
from both networks — instead of testing the next guess.

## Anything over 3 hours

| # | Task | Hours | Why it is not shorter |
|---|---|---|---|
| 4 | PYQ for classes 6–8 | 8 | Not code. Content behind a session-gated API, then a mapping to `sections.type_key` and an import run per class |
| 5 | Class 10 revision notes | 6 | Same shape as the PYQ import, smaller scope — 274 chapters short |
| 7 | Close the maths DB gap | 6 | ~71% of `maths_questions` is not in the DB. The bundled files are the source, so this is an import, but the chapter mapping has to be right or it lands under chapters nothing queries |
| 9 | Bundled banks → API | 8 | Migration plus changes to the screens that read them. The DB side is largely done |
| 13 | Cross-class QA | 4 | Seven classes across five surfaces, on a device, checking the paid lock each time |
| 17 | Parental consent | 10 | A product change: age capture, a parent contact step, verification, and a consent record |
| 18 | Support backend | 40 | Agent console, ticket routing, presence, notifications |

## Fixed since the 31 August list

Not open work — recorded so the two documents can be compared.

In-app account deletion · support chat no longer implies a person · any-class
browsing (server and app) · `access_level` dropped at sign-in, which made every
granted account still look locked · unlock requests never reaching the support
queue · the Grant button giving no reason when it hid itself · "Select your
profile" asked twice · the Resources tab freezing on 5 MB of eager imports, and
the Practice tab on 45 MB (found while compiling this list) · practice content
from 16,251 questions to 88,950 · password reset built end to end · 118 MB of
verified-duplicate data removed.

Closed since this list was written on 4 September: tab stalls — 8171a62 + a82dfd1, shipped in the 5 Sep OTA · grievance officer — the live page reads "Saurabh, saurabh@ailernova.com"; the earlier [NAME] read was a cached copy · web admin access toggle — aba168b · Terms of Use — fd40629, shipped in the same OTA.
