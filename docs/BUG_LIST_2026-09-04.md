# Ailernova — open bugs and tasks

**4 September 2026**, revised 5 September · `main` at `fd40629` · OTA `9f0d5539` live on `production` · Play build 6 (`fed845f`, 21 Aug), 88 commits behind

Every row was checked against the live database, the deployed server, or the code
— not carried over from the previous list. Items fixed since 31 August are listed
separately at the end rather than restated as open.

---

| # | Bug / task to be fixed | What it is | Priority | Est. hours |
|---|---|---|---|---|
| 1 | **No mail transport configured** | Password reset is built and tested but sends nothing: neither `SMTP_HOST` nor `RESEND_API_KEY` is set on Render. `/forgot-password` still answers "a reset link is on its way", so the failure is invisible to users and to the app | P0 | 0.25 |
| 2 | **`ELEVENLABS_API_KEY` not set** | Every AI-teacher lesson falls back to the device voice. There is no fallback chain by design, so one missing key silently changes the teacher's voice for everyone | P0 | 0.25 |
| 3 | **Forgot password does not work end to end** | The screen, the endpoints, the emailed link and the reset form are all built and tested, but no mail transport is configured, so nothing is ever sent (see task 2). Needs an SMTP or Resend credential, then one pass through the real flow on a device | P0 | 0.5 |
| 4 | **No PYQ for classes 6–8** | Class 7 has 0 of 327 chapters, class 8 0 of 342, class 6 just 11 of 292. Needs an examin8 fetch, which is session-gated — blocked on a login | P1 | 8 |
| 5 | **Class 10 revision notes cover 44 of 318 chapters** | Classes 6–9 are fine via `ncert_solutions` part 4; class 10 uses the `sections` model and is mostly empty | P1 | 6 |
| 6 | **Hostinger account lockout** | `ailernova.com` sits in an account nobody can sign into. It blocks Resend domain verification, and the domain **expires 13 October 2026** — after which the site, the Microsoft mail and the API host references all lapse | P1 | 2 |
| 7 | **Maths content mostly missing from the DB** | `maths_questions` is ~29% present, against biology ~100%, physics ~95%, chemistry ~80%. The bundled copy is the only copy for the rest, so it must not be deleted and the gap should be closed | P1 | 6 |
| 8 | **Play still serves build 6** | Deferred by decision — staying on build 6 for now. Everything since is JavaScript, so the OTA delivers it to build 6 users, including in-app account deletion (JS + server, no native code). What OTA cannot carry is one line added after build 9, `"scheme": "ailernova"`, which nothing currently uses. The residual risk is a Play reviewer's FIRST launch: `fallbackToCacheTimeout: 0` serves the cached bundle once before applying the update, so a fresh install opened once shows build 6's original JS, without the deletion flow. An AAB is already built and waiting (version code 9, finished 29 Aug, runtime 1.0.1) if that ever needs closing | P2 | 0.2 |
| 9 | **Bundle is 51.8 MB** | The reachable question banks ship inside it. Moving them to the API is the fix; the DB now holds 88,950 practice questions, so most of the destination already exists | P2 | 8 |
| 10 | **~215 MB of `src/data` is unreachable from any screen** | Removable only after a both-sides count against the DB, and never for maths | P2 | 3 |
| 11 | **Errors swallowed silently** | 43 empty `catch {}` blocks across app and server. This is why the chapter-list bug took so long to find | P2 | 2 |
| 12 | **Debug logging in shipping code** | 69 `console.log` calls left in the app | P2 | 0.5 |
| 13 | **Any-class browsing has not been tested on a device** | Server and app are both live; needs a pass across Practice, Resources, MCQ, online and mock tests for each class, confirming content actually changes and the free/paid lock is untouched | P2 | 4 |
| 14 | **Mr. Iker is an illustration, not the HeyGen avatar** | The male teacher is drawn in SVG. Swapping in avatar `214e2370…` needs a HeyGen API key, or any male portrait via `--from` | P2 | 0.5 |
| 15 | **Test data in production** | `grant-test@ailernova.test` and ticket `AL-2549` were created to prove the grant flow and are still live | P2 | 0.1 |
| 16 | **Releases are uploaded by hand** | `eas submit` with a Play service account would stop the next 75-commit gap forming | P2 | 1 |
| 17 | **No age gate or verifiable parental consent** | The DPDP Act requires it for under-18s, and most users are children. Product and legal input needed before this can be scoped properly | P3 | 10 |
| 18 | **Support chat reaches nobody outside working hours** | Task 2 made the copy honest; an agent console, routing and presence is the real feature | P3 | 40 |

**Totals** — P0 1 · P1 22 · P2 19.3 · P3 50 · **92.3 hours**, or **52.3** excluding the 40-hour support backend.

---

## Where the leverage is

The whole P0 block is **1 hour** and none of it is coding — it is three
configuration values and a name in a policy. Everything else written this week
already reaches users: the OTA on 5 September carries the tab-stall fixes,
account deletion, Terms of Use, practice for five classes and any-class
browsing to build 6 without a new release.

## Anything over 3 hours

| # | Task | Hours | Why it is not shorter |
|---|---|---|---|
| 4 | PYQ for classes 6–8 | 8 | Not code. Content behind a session-gated API, then a mapping to `sections.type_key` and an import run per class |
| 5 | Class 10 revision notes | 6 | Same shape as the PYQ import, smaller scope — 274 chapters short |
| 7 | Close the maths DB gap | 6 | ~71% of `maths_questions` is not in the DB. The bundled files are the source, so this is an import, but the chapter mapping has to be right or it lands under chapters nothing queries |
| 9 | Bundled banks → API | 8 | Migration plus changes to the screens that read them. The DB side is largely done |
| 13 | Cross-class QA | 4 | Seven classes across five surfaces, on a device, checking the paid lock each time |
| 17 | Parental consent | 10 | A product change: age capture, a parent contact step, verification, and a consent record |
| 18 | Support backend | 40 | Agent console, ticket routing, presence, notifications |

## Fixed since the 31 August list

Not open work — recorded so the two documents can be compared.

In-app account deletion · support chat no longer implies a person · any-class
browsing (server and app) · `access_level` dropped at sign-in, which made every
granted account still look locked · unlock requests never reaching the support
queue · the Grant button giving no reason when it hid itself · "Select your
profile" asked twice · the Resources tab freezing on 5 MB of eager imports, and
the Practice tab on 45 MB (found while compiling this list) · practice content
from 16,251 questions to 88,950 · password reset built end to end · 118 MB of
verified-duplicate data removed.

Closed since this list was written on 4 September: tab stalls — 8171a62 + a82dfd1, shipped in the 5 Sep OTA · grievance officer — the live page reads "Saurabh, saurabh@ailernova.com"; the earlier [NAME] read was a cached copy · web admin access toggle — aba168b · Terms of Use — fd40629, shipped in the same OTA.
